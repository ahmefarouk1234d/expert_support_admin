import 'package:flutter/material.dart';
import 'request_change_orders.dart';

class RequestChangeInbox extends StatelessWidget {
  static const route = "/RequestChangeInbox";

  @override
  Widget build(BuildContext context) {
    return RequestChangeOrder();
  }
}