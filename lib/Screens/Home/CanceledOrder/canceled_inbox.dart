import 'package:flutter/material.dart';
import 'canceled_orders.dart';

class CancelecInbox extends StatelessWidget {
  static const route = "/DoneInbox";

  @override
  Widget build(BuildContext context) {
    return CanceledOrder();
  }
}