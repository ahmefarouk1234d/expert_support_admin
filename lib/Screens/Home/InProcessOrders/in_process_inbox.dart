import 'package:flutter/material.dart';
import 'in_process_orders.dart';

class InProcessInbox extends StatelessWidget {
  static const route = "/InProcessInbox";

  @override
  Widget build(BuildContext context) {
    return InProcessOrder();
  }
}