import 'package:flutter/material.dart';
import 'pending_orders.dart';

class PendingInbox extends StatelessWidget {
  static const route = "/PendingInbox";

  @override
  Widget build(BuildContext context) {
    return PendingOrder();
  }
}