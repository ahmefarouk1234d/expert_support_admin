import 'package:flutter/material.dart';
import 'done_orders.dart';

class DoneInbox extends StatelessWidget {
  static const route = "/DoneInbox";

  @override
  Widget build(BuildContext context) {
    return DoneOrder();
  }
}