enum eOrderStatus{
  pending,
  in_process,
  done,
  canceled,
  unknown
}

enum AuthStatus{
  signedIn,
  notSingedIn
}

enum OrderToDisplay{
  pending,
  inProcess,
  done,
  canceled,
  all
}

enum GeneralDetailsType {
  contactUs,
  shared,
  submitOrder,
  orderLimit
}